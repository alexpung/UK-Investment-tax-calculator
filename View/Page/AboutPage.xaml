<ui:UiPage
    x:Class="CapitalGainCalculator.View.Page.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:viewmodel="clr-namespace:CapitalGainCalculator.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:AboutViewModel}"
    Title="LoadDataPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    mc:Ignorable="d">
    <Grid x:Name="LoadDataPageMainStackPanel" Margin="20,20,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" x:Name="VersionText" Content="{Binding GetVersion}"/>
        <TextBox Grid.Row="1" x:Name="CopyrightText" Text="{Binding CopyrightNotice, Mode=OneTime}" IsReadOnly="True" TextWrapping="Wrap"/>
    </Grid>
</ui:UiPage>
